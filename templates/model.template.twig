import mongoose from 'mongoose';

const {{ modelName|capitalize }}Schema = new mongoose.Schema({

});

const {{ modelName|capitalize }} = mongoose.model('{{ modelName|capitalize }}', {{ modelName|capitalize }}Schema);
export default {{ modelName|capitalize }};
